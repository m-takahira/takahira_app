<%= form_for(@login) do |f| %>
  <% if @login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login.errors.count, "error") %> prohibited this login from being saved:</h2>

      <ul>
      <% @login.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name ,class:"btn btn-default"%>
  </div>
  <div class="field">
    <%= f.label :user_password %><br>
    <%= f.password_field :user_password ,class:"btn btn-default"%>
  </div>
  <div class="actions">
    <%= link_to 'Login', posts_path ,class:"btn btn-default"%>
  </div>
<% end %>
